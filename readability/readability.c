#include <cs50.h>
#include <stdio.h>

int main(void)
{
    string input = get_string("Text: ");
    int i = 0, letters = 0, words = 0, sentences = 0;

    do
    {
        //count letters
        if (input[i] > )

        //cycle through chars in input
        i++;
    }
    while (input[i] != '\0');
}